import { FC } from 'react'

interface FooterProps {

}

const Footer: FC<FooterProps> = ({ }) => {
    return (
        <footer className='w-full py-4 text-center text-primary-foreground text-xs bg-slate-900'>
            TODO: Footer content
        </footer>
    )
}

export default Footer